"""
sumanyu_rao_composer_pipeline_dag.py
─────────────────────────────────────────────────────────────────────────────
Airflow DAG owned by: Sumanyu Rao
Dataset  : sumanyu_rao_composer (BigQuery)
Tables   : composer_configuration | dag_runs | task_instances
 
Pipeline steps
  1. insert_composer_config  — seed Composer environment snapshot
  2. insert_dag_run          — record this very DAG run into dag_runs
  3. insert_task_instances   — record task-level metrics
  4. verify_records          — SELECT from all 3 tables and log row counts (SLI)
─────────────────────────────────────────────────────────────────────────────
"""
 
from __future__ import annotations
 
import json
import uuid
from datetime import datetime, timedelta, timezone
 
from airflow import DAG
from airflow.providers.google.cloud.operators.bigquery import BigQueryInsertJobOperator
from airflow.providers.google.cloud.hooks.bigquery import BigQueryHook
from airflow.operators.python import PythonOperator
from airflow.utils.dates import days_ago
 
